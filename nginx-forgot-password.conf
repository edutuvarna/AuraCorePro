# Forgot Password clean URL
location = /forgot-password {
    try_files /forgot-password.html =404;
}

# Redirect .html to clean URL
location = /forgot-password.html {
    return 301 /forgot-password;
}
