# Phase 6.3 Light Theme Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship Dark/Light/System theme switcher with full design-token duality + Phase 6.2 gallery Light-variant pixel regression coverage.

**Architecture:** 5 sub-waves. 6.3.A adds the Light token dictionary alongside the existing Dark one in `AuraCoreThemeV2.axaml`. 6.3.B refactors `ThemeService` from 2-state bool to 3-state `AppTheme` enum with OS-theme detection + persistence migration. 6.3.C replaces the 2-state toggle in `SettingsView` with a radio group + removes the hardcoded `RequestedThemeVariant="Dark"` from `App.axaml`. 6.3.D adds 2 Light-variant pixel tests to the Phase 6.2 `DesignSystemGallery`. 6.3.E closes with memory + ceremonial commit.

**Tech Stack:** .NET 8 · C# 12 · Avalonia UI 11.2.7 (`ThemeDictionaries` + `ThemeVariant.{Dark,Light,Default}`) · xUnit · Verify.Xunit 28.1.0 + Verify.ImageSharp 4.0.0 (Phase 6.2 infra)

**Spec reference:** `docs/superpowers/specs/2026-04-18-phase6.3-light-theme-design.md` (committed at branch creation)

**Baseline:** Branch `phase-6-light-theme` from main `f931020`. 2252 passing + 0 skipped + 0 failed. Target at close: **2254 passing** (+2 Light gallery tests).

---

## File Structure

### Created

```
tests/AuraCore.Tests.UI.Avalonia/PixelRegression/DesignSystemGalleryLightPixelTests.cs
tests/AuraCore.Tests.UI.Avalonia/goldens/DesignSystemGalleryLightPixelTests.Gallery_wide_light.verified.png
tests/AuraCore.Tests.UI.Avalonia/goldens/DesignSystemGalleryLightPixelTests.Gallery_wide_light.verified.txt
tests/AuraCore.Tests.UI.Avalonia/goldens/DesignSystemGalleryLightPixelTests.Gallery_narrow_light.verified.png
tests/AuraCore.Tests.UI.Avalonia/goldens/DesignSystemGalleryLightPixelTests.Gallery_narrow_light.verified.txt
tests/AuraCore.Tests.UI.Avalonia/Services/ThemeServiceTests.cs
```

### Modified

```
src/UI/AuraCore.UI.Avalonia/Themes/AuraCoreThemeV2.axaml          (+Light dictionary, glow split)
src/UI/AuraCore.UI.Avalonia/ThemeService.cs                       (3-state refactor)
src/UI/AuraCore.UI.Avalonia/App.axaml                             (remove hardcoded Dark)
src/UI/AuraCore.UI.Avalonia/Views/Pages/SettingsView.axaml        (radio group replaces toggle)
src/UI/AuraCore.UI.Avalonia/Views/Pages/SettingsView.axaml.cs     (3-state handler)
```

---

# Sub-wave 6.3.A — Light token dictionary

## Task 1: Add Light `ThemeDictionary` + move glows into per-theme dicts

**Files:**
- Modify: `src/UI/AuraCore.UI.Avalonia/Themes/AuraCoreThemeV2.axaml`

This is pure XAML. Two changes happen together:

1. Add a `<ResourceDictionary x:Key="Light">` sibling to the existing Dark one, with Light values for every dark token.
2. Move the six `Glow*` `BoxShadows` definitions (currently in the shared outer `ResourceDictionary`) into per-theme `ThemeDictionaries` with dimmed alphas for Light.

- [ ] **Step 1: Read the current file to confirm structure**

```bash
cd C:/Users/Admin/Desktop/AuraCorePro/AuraCorePro
cat src/UI/AuraCore.UI.Avalonia/Themes/AuraCoreThemeV2.axaml
```

Confirm:
- `<Styles.Resources>` → `<ResourceDictionary>` → `<ResourceDictionary.ThemeDictionaries>` wraps a single `<ResourceDictionary x:Key="Dark">` block ending at line ~73.
- `Glow*` BoxShadows are in the outer `ResourceDictionary` (lines ~109-114), OUTSIDE `ThemeDictionaries`.

- [ ] **Step 2: Replace the file contents with the Dark+Light dual-dictionary version**

Write the complete file below:

```xml
<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- ══════════════════════════════════════════════════════
       AuraCore Pro V2 Design System
       Source: docs/superpowers/specs/2026-04-14-ui-rebuild-vision-document.md
       Dark + Light as of Phase 6.3. Modern glassmorphism (dark) +
       clean off-white surfaces (light), cross-platform.
       ══════════════════════════════════════════════════════ -->

  <Styles.Resources>
    <ResourceDictionary>
      <ResourceDictionary.ThemeDictionaries>

        <!-- ╔═ DARK ═══════════════════════════════════════════╗ -->
        <ResourceDictionary x:Key="Dark">

          <!-- Backgrounds -->
          <SolidColorBrush x:Key="BgDeepBrush" Color="#0A0A10"/>
          <SolidColorBrush x:Key="BgSurfaceBrush" Color="#0E0E14"/>
          <SolidColorBrush x:Key="BgSidebarBrush" Color="#0B0B11"/>
          <SolidColorBrush x:Key="BgCardBrush" Color="#06FFFFFF"/>
          <SolidColorBrush x:Key="BgCardElevatedBrush" Color="#0AFFFFFF"/>

          <!-- Borders -->
          <SolidColorBrush x:Key="BorderSubtleBrush" Color="#0FFFFFFF"/>
          <SolidColorBrush x:Key="BorderEmphasisBrush" Color="#14FFFFFF"/>

          <!-- Text -->
          <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F0F0F5"/>
          <SolidColorBrush x:Key="TextSecondaryBrush" Color="#E8E8F0"/>
          <SolidColorBrush x:Key="TextMutedBrush" Color="#888899"/>
          <SolidColorBrush x:Key="TextDisabledBrush" Color="#555570"/>

          <!-- Accent: Teal -->
          <SolidColorBrush x:Key="AccentTealBrush" Color="#00D4AA"/>
          <SolidColorBrush x:Key="AccentTealLightBrush" Color="#6CE0C0"/>
          <SolidColorBrush x:Key="AccentTealDimBrush" Color="#1400D4AA"/>

          <!-- Accent: Purple -->
          <SolidColorBrush x:Key="AccentPurpleBrush" Color="#B088FF"/>
          <SolidColorBrush x:Key="AccentPurpleDeepBrush" Color="#8B5CF6"/>
          <SolidColorBrush x:Key="AccentPurpleDimBrush" Color="#0F8B5CF6"/>

          <!-- Accent: Amber -->
          <SolidColorBrush x:Key="AccentAmberBrush" Color="#F59E0B"/>
          <SolidColorBrush x:Key="AccentAmberDimBrush" Color="#14F59E0B"/>

          <!-- Accent: Pink -->
          <SolidColorBrush x:Key="AccentPinkBrush" Color="#EC4899"/>
          <SolidColorBrush x:Key="AccentPinkDimBrush" Color="#14EC4899"/>

          <!-- Semantic -->
          <SolidColorBrush x:Key="StatusSuccessBrush" Color="#00D4AA"/>
          <SolidColorBrush x:Key="StatusWarningBrush" Color="#F59E0B"/>
          <SolidColorBrush x:Key="StatusErrorBrush" Color="#EF4444"/>
          <SolidColorBrush x:Key="StatusInfoBrush" Color="#B088FF"/>

          <!-- Tinted semantic surfaces (fixed in Phase 6.2, AARRGGBB order) -->
          <SolidColorBrush x:Key="SuccessBgBrush" Color="#2022C55E"/>
          <SolidColorBrush x:Key="SuccessBorderBrush" Color="#3022C55E"/>
          <SolidColorBrush x:Key="WarningBgBrush" Color="#0DF59E0B"/>
          <SolidColorBrush x:Key="WarningBorderBrush" Color="#33F59E0B"/>
          <SolidColorBrush x:Key="ErrorBgBrush" Color="#20EF4444"/>
          <SolidColorBrush x:Key="ErrorBorderBrush" Color="#30EF4444"/>
          <SolidColorBrush x:Key="InfoBgBrush" Color="#0D3B82F6"/>
          <SolidColorBrush x:Key="InfoBorderBrush" Color="#203B82F6"/>
          <SolidColorBrush x:Key="AccentBgBrush" Color="#0D00D4AA"/>
          <SolidColorBrush x:Key="AccentBorderBrush" Color="#2000D4AA"/>

          <!-- Action button -->
          <SolidColorBrush x:Key="ActionBtnBgBrush" Color="#12FFFFFF"/>
          <SolidColorBrush x:Key="ActionBtnHoverBrush" Color="#1500D4AA"/>

          <!-- Glow shadows (per-theme — Dark is full-strength) -->
          <BoxShadows x:Key="GlowTeal">0 0 20 0 #3300D4AA</BoxShadows>
          <BoxShadows x:Key="GlowPurple">0 0 20 0 #33B088FF</BoxShadows>
          <BoxShadows x:Key="GlowAmber">0 0 20 0 #2EF59E0B</BoxShadows>
          <BoxShadows x:Key="GlowPink">0 0 20 0 #33EC4899</BoxShadows>
          <BoxShadows x:Key="GlowHealth">0 0 28 0 #6600D4AA</BoxShadows>
          <BoxShadows x:Key="GlowHero">0 0 32 0 #1A00D4AA</BoxShadows>

        </ResourceDictionary>

        <!-- ╔═ LIGHT ══════════════════════════════════════════╗ -->
        <ResourceDictionary x:Key="Light">

          <!-- Backgrounds (off-white surfaces, Tailwind-aligned) -->
          <SolidColorBrush x:Key="BgDeepBrush" Color="#F8F8FA"/>
          <SolidColorBrush x:Key="BgSurfaceBrush" Color="#FFFFFF"/>
          <SolidColorBrush x:Key="BgSidebarBrush" Color="#F3F3F7"/>
          <SolidColorBrush x:Key="BgCardBrush" Color="#08000000"/>
          <SolidColorBrush x:Key="BgCardElevatedBrush" Color="#10000000"/>

          <!-- Borders (black-alpha mirrors white-alpha in dark) -->
          <SolidColorBrush x:Key="BorderSubtleBrush" Color="#0F000000"/>
          <SolidColorBrush x:Key="BorderEmphasisBrush" Color="#14000000"/>

          <!-- Text (inverted) -->
          <SolidColorBrush x:Key="TextPrimaryBrush" Color="#0F0F15"/>
          <SolidColorBrush x:Key="TextSecondaryBrush" Color="#1A1A23"/>
          <SolidColorBrush x:Key="TextMutedBrush" Color="#6B6B7A"/>
          <SolidColorBrush x:Key="TextDisabledBrush" Color="#A0A0B0"/>

          <!-- Accent: Teal (darker tone for contrast on white) -->
          <SolidColorBrush x:Key="AccentTealBrush" Color="#00D4AA"/>
          <SolidColorBrush x:Key="AccentTealLightBrush" Color="#00A888"/>
          <SolidColorBrush x:Key="AccentTealDimBrush" Color="#2000D4AA"/>

          <!-- Accent: Purple -->
          <SolidColorBrush x:Key="AccentPurpleBrush" Color="#8B5CF6"/>
          <SolidColorBrush x:Key="AccentPurpleDeepBrush" Color="#6D3FE6"/>
          <SolidColorBrush x:Key="AccentPurpleDimBrush" Color="#1A8B5CF6"/>

          <!-- Accent: Amber -->
          <SolidColorBrush x:Key="AccentAmberBrush" Color="#D97706"/>
          <SolidColorBrush x:Key="AccentAmberDimBrush" Color="#20F59E0B"/>

          <!-- Accent: Pink -->
          <SolidColorBrush x:Key="AccentPinkBrush" Color="#DB2777"/>
          <SolidColorBrush x:Key="AccentPinkDimBrush" Color="#20EC4899"/>

          <!-- Semantic (Tailwind -600 for visibility on light bg) -->
          <SolidColorBrush x:Key="StatusSuccessBrush" Color="#059669"/>
          <SolidColorBrush x:Key="StatusWarningBrush" Color="#D97706"/>
          <SolidColorBrush x:Key="StatusErrorBrush" Color="#DC2626"/>
          <SolidColorBrush x:Key="StatusInfoBrush" Color="#8B5CF6"/>

          <!-- Tinted semantic surfaces (higher alpha for light-bg visibility) -->
          <SolidColorBrush x:Key="SuccessBgBrush" Color="#3022C55E"/>
          <SolidColorBrush x:Key="SuccessBorderBrush" Color="#4022C55E"/>
          <SolidColorBrush x:Key="WarningBgBrush" Color="#20F59E0B"/>
          <SolidColorBrush x:Key="WarningBorderBrush" Color="#40F59E0B"/>
          <SolidColorBrush x:Key="ErrorBgBrush" Color="#30EF4444"/>
          <SolidColorBrush x:Key="ErrorBorderBrush" Color="#40EF4444"/>
          <SolidColorBrush x:Key="InfoBgBrush" Color="#203B82F6"/>
          <SolidColorBrush x:Key="InfoBorderBrush" Color="#303B82F6"/>
          <SolidColorBrush x:Key="AccentBgBrush" Color="#2000D4AA"/>
          <SolidColorBrush x:Key="AccentBorderBrush" Color="#3000D4AA"/>

          <!-- Action button -->
          <SolidColorBrush x:Key="ActionBtnBgBrush" Color="#12000000"/>
          <SolidColorBrush x:Key="ActionBtnHoverBrush" Color="#2500D4AA"/>

          <!-- Glow shadows (dimmed for light — dark glows look like smudges) -->
          <BoxShadows x:Key="GlowTeal">0 0 20 0 #1400D4AA</BoxShadows>
          <BoxShadows x:Key="GlowPurple">0 0 20 0 #14B088FF</BoxShadows>
          <BoxShadows x:Key="GlowAmber">0 0 20 0 #14F59E0B</BoxShadows>
          <BoxShadows x:Key="GlowPink">0 0 20 0 #14EC4899</BoxShadows>
          <BoxShadows x:Key="GlowHealth">0 0 28 0 #2000D4AA</BoxShadows>
          <BoxShadows x:Key="GlowHero">0 0 32 0 #0A00D4AA</BoxShadows>

        </ResourceDictionary>

      </ResourceDictionary.ThemeDictionaries>

      <!-- ── SHARED (theme-independent) TOKENS ────── -->

      <!-- Typography: font family stack -->
      <FontFamily x:Key="PrimaryFontFamily">Inter, Segoe UI Variable, Segoe UI, sans-serif</FontFamily>

      <!-- Typography: sizes (double) -->
      <x:Double x:Key="FontSizeDisplay">24</x:Double>
      <x:Double x:Key="FontSizeHeading">18</x:Double>
      <x:Double x:Key="FontSizeSubheading">14</x:Double>
      <x:Double x:Key="FontSizeBody">12</x:Double>
      <x:Double x:Key="FontSizeBodySmall">11</x:Double>
      <x:Double x:Key="FontSizeLabel">10</x:Double>
      <x:Double x:Key="FontSizeCaption">9</x:Double>

      <!-- Smaller typography for micro-UI elements -->
      <x:Double x:Key="FontSizeMicro">8</x:Double>
      <x:Double x:Key="FontSizeTiny">7</x:Double>
      <x:Double x:Key="FontSizeLarge">14</x:Double>

      <!-- Radius scale -->
      <CornerRadius x:Key="RadiusXs">4</CornerRadius>
      <CornerRadius x:Key="RadiusSm">6</CornerRadius>
      <CornerRadius x:Key="RadiusMd">8</CornerRadius>
      <CornerRadius x:Key="RadiusLg">12</CornerRadius>
      <CornerRadius x:Key="RadiusXl">14</CornerRadius>

      <!-- Spacing (Thickness) -->
      <Thickness x:Key="SpacingCardPadding">14</Thickness>
      <Thickness x:Key="SpacingPagePadding">22,18</Thickness>
      <Thickness x:Key="SpacingGapSm">0,6</Thickness>
      <Thickness x:Key="SpacingGapMd">0,10</Thickness>
      <Thickness x:Key="SpacingGapLg">0,14</Thickness>
    </ResourceDictionary>
  </Styles.Resources>

  <!-- Apply Inter font globally via FluentTheme-aware selectors -->
  <Style Selector="Window">
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFontFamily}"/>
  </Style>
  <Style Selector="TextBlock">
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFontFamily}"/>
  </Style>
  <Style Selector="TextBox">
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFontFamily}"/>
  </Style>
  <Style Selector="Button">
    <Setter Property="FontFamily" Value="{DynamicResource PrimaryFontFamily}"/>
  </Style>

  <!-- AIFeaturesView section navigation buttons -->
  <Style Selector="Button.section-nav-item">
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="FontSize" Value="11" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
    <Setter Property="Cursor" Value="Hand" />
  </Style>
  <Style Selector="Button.section-nav-item:pointerover /template/ ContentPresenter">
    <Setter Property="Background" Value="#10FFFFFF" />
  </Style>

  <!-- ── ACTION BUTTON STYLE (ported from V1 bridge 2026-04-16) ─── -->
  <Style Selector="Button.action-btn">
    <Setter Property="Background" Value="{DynamicResource ActionBtnBgBrush}" />
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Padding" Value="12,9" />
    <Setter Property="CornerRadius" Value="{DynamicResource RadiusMd}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Cursor" Value="Hand" />
  </Style>
  <Style Selector="Button.action-btn:pointerover /template/ ContentPresenter">
    <Setter Property="Background" Value="{DynamicResource ActionBtnHoverBrush}" />
  </Style>
</Styles>
```

- [ ] **Step 3: Build**

```bash
dotnet build src/UI/AuraCore.UI.Avalonia/AuraCore.UI.Avalonia.csproj --nologo 2>&1 | tail -6
```

Expected: 0 errors. Warnings OK.

- [ ] **Step 4: Full test suite**

```bash
dotnet test AuraCorePro.sln --verbosity minimal --nologo 2>&1 | grep -E "^(Passed!|Failed!)" | tail -10
```

Expected: 8 `Passed!` lines, 0 `Failed!`, still 2252 total (no new tests yet). The existing Phase 6.2 gallery test is still Dark-themed and passes because it uses the same Dark tokens.

- [ ] **Step 5: Commit**

```bash
git add src/UI/AuraCore.UI.Avalonia/Themes/AuraCoreThemeV2.axaml
git commit -m "feat(phase-6.3.A): Light token dictionary + per-theme glow split"
```

---

# Sub-wave 6.3.B — ThemeService 3-state refactor

## Task 2: `ThemeService` refactor — AppTheme enum, System resolve, persistence migration

**Files:**
- Modify: `src/UI/AuraCore.UI.Avalonia/ThemeService.cs`
- Create: `tests/AuraCore.Tests.UI.Avalonia/Services/ThemeServiceTests.cs`

TDD: write tests first, then refactor implementation to pass.

- [ ] **Step 1: Create the test file**

Path: `tests/AuraCore.Tests.UI.Avalonia/Services/ThemeServiceTests.cs`

```csharp
using System;
using System.IO;
using AuraCore.UI.Avalonia;
using Avalonia.Headless.XUnit;
using Avalonia.Styling;
using Xunit;

namespace AuraCore.Tests.UI.Avalonia.Services;

public class ThemeServiceTests
{
    // Note: ThemeService is static with real filesystem persistence.
    // Tests share process state; use SetTheme to restore at finally.

    [AvaloniaFact]
    public void CurrentTheme_default_is_Dark()
    {
        // Startup default (pre-Initialize) is Dark.
        Assert.Equal(ThemeService.AppTheme.Dark, ThemeService.CurrentTheme);
    }

    [AvaloniaFact]
    public void SetTheme_Light_sets_EffectiveVariant_to_Light()
    {
        var prev = ThemeService.CurrentTheme;
        try
        {
            ThemeService.SetTheme(ThemeService.AppTheme.Light);
            Assert.Equal(ThemeService.AppTheme.Light, ThemeService.CurrentTheme);
            Assert.Equal(ThemeVariant.Light, ThemeService.EffectiveVariant);
        }
        finally
        {
            ThemeService.SetTheme(prev);
        }
    }

    [AvaloniaFact]
    public void SetTheme_Dark_sets_EffectiveVariant_to_Dark()
    {
        var prev = ThemeService.CurrentTheme;
        try
        {
            ThemeService.SetTheme(ThemeService.AppTheme.Dark);
            Assert.Equal(ThemeService.AppTheme.Dark, ThemeService.CurrentTheme);
            Assert.Equal(ThemeVariant.Dark, ThemeService.EffectiveVariant);
        }
        finally
        {
            ThemeService.SetTheme(prev);
        }
    }

    [AvaloniaFact]
    public void SetTheme_System_resolves_EffectiveVariant_to_Dark_or_Light()
    {
        var prev = ThemeService.CurrentTheme;
        try
        {
            ThemeService.SetTheme(ThemeService.AppTheme.System);
            Assert.Equal(ThemeService.AppTheme.System, ThemeService.CurrentTheme);
            // System resolves to one of the concrete variants — never Default.
            Assert.True(
                ThemeService.EffectiveVariant == ThemeVariant.Dark ||
                ThemeService.EffectiveVariant == ThemeVariant.Light,
                $"EffectiveVariant was {ThemeService.EffectiveVariant}, expected Dark or Light");
        }
        finally
        {
            ThemeService.SetTheme(prev);
        }
    }

    [AvaloniaFact]
    public void IsDarkMode_reflects_EffectiveVariant()
    {
        var prev = ThemeService.CurrentTheme;
        try
        {
            ThemeService.SetTheme(ThemeService.AppTheme.Light);
            Assert.False(ThemeService.IsDarkMode);

            ThemeService.SetTheme(ThemeService.AppTheme.Dark);
            Assert.True(ThemeService.IsDarkMode);
        }
        finally
        {
            ThemeService.SetTheme(prev);
        }
    }

    [AvaloniaFact]
    public void Toggle_cycles_Dark_to_Light_to_Dark_skipping_System()
    {
        var prev = ThemeService.CurrentTheme;
        try
        {
            ThemeService.SetTheme(ThemeService.AppTheme.Dark);
            ThemeService.Toggle();
            Assert.Equal(ThemeService.AppTheme.Light, ThemeService.CurrentTheme);

            ThemeService.Toggle();
            Assert.Equal(ThemeService.AppTheme.Dark, ThemeService.CurrentTheme);
        }
        finally
        {
            ThemeService.SetTheme(prev);
        }
    }

    [AvaloniaFact]
    public void ThemeChanged_fires_when_theme_changes()
    {
        var prev = ThemeService.CurrentTheme;
        ThemeService.SetTheme(ThemeService.AppTheme.Dark);
        var fired = 0;
        Action handler = () => fired++;
        ThemeService.ThemeChanged += handler;
        try
        {
            ThemeService.SetTheme(ThemeService.AppTheme.Light);
            Assert.Equal(1, fired);

            // Redundant set to same theme — no fire expected.
            ThemeService.SetTheme(ThemeService.AppTheme.Light);
            Assert.Equal(1, fired);

            ThemeService.SetTheme(ThemeService.AppTheme.Dark);
            Assert.Equal(2, fired);
        }
        finally
        {
            ThemeService.ThemeChanged -= handler;
            ThemeService.SetTheme(prev);
        }
    }
}
```

- [ ] **Step 2: Run tests (expect 7 failures — API shape doesn't exist yet)**

```bash
dotnet test tests/AuraCore.Tests.UI.Avalonia/AuraCore.Tests.UI.Avalonia.csproj --filter "FullyQualifiedName~ThemeServiceTests" --nologo 2>&1 | tail -10
```

Expected: build error (CS0117 "does not contain a definition for 'AppTheme'" etc.) OR 7 test fails.

- [ ] **Step 3: Rewrite `ThemeService.cs` to pass**

Path: `src/UI/AuraCore.UI.Avalonia/ThemeService.cs`

```csharp
using global::Avalonia;
using global::Avalonia.Styling;

namespace AuraCore.UI.Avalonia;

/// <summary>
/// Manages application theme (Dark / Light / System) and persists preference.
/// Persistence file: %LOCALAPPDATA%/AuraCorePro/theme.pref ("dark"|"light"|"system").
/// </summary>
public static class ThemeService
{
    public enum AppTheme { Dark, Light, System }

    private static readonly string PrefPath = Path.Combine(
        Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),
        "AuraCorePro", "theme.pref");

    public static AppTheme CurrentTheme { get; private set; } = AppTheme.Dark;

    /// <summary>
    /// Actual ThemeVariant applied to Application.Current. For AppTheme.System,
    /// this resolves to ThemeVariant.Dark or ThemeVariant.Light via OS detection.
    /// </summary>
    public static ThemeVariant EffectiveVariant { get; private set; } = ThemeVariant.Dark;

    /// <summary>Back-compat shim. True when EffectiveVariant is Dark.</summary>
    public static bool IsDarkMode => EffectiveVariant == ThemeVariant.Dark;

    public static event Action? ThemeChanged;

    /// <summary>Load saved preference (call once at startup, before MainWindow shows).</summary>
    public static void Initialize()
    {
        var loaded = AppTheme.Dark;
        try
        {
            if (File.Exists(PrefPath))
            {
                var content = File.ReadAllText(PrefPath).Trim().ToLowerInvariant();
                loaded = content switch
                {
                    "light" => AppTheme.Light,
                    "system" => AppTheme.System,
                    _ => AppTheme.Dark,
                };
            }
        }
        catch { /* default to dark */ }

        CurrentTheme = loaded;
        ApplyTheme();
    }

    /// <summary>Set specific theme. Persists to disk and fires ThemeChanged.</summary>
    public static void SetTheme(AppTheme theme)
    {
        if (CurrentTheme == theme) return;
        CurrentTheme = theme;
        ApplyTheme();
        Save();
        ThemeChanged?.Invoke();
    }

    /// <summary>Toggle Dark ↔ Light. Skips System (user must pick it explicitly).</summary>
    public static void Toggle()
    {
        SetTheme(CurrentTheme == AppTheme.Dark ? AppTheme.Light : AppTheme.Dark);
    }

    private static void ApplyTheme()
    {
        EffectiveVariant = CurrentTheme switch
        {
            AppTheme.Dark => ThemeVariant.Dark,
            AppTheme.Light => ThemeVariant.Light,
            AppTheme.System => ResolveSystemVariant(),
            _ => ThemeVariant.Dark,
        };

        if (global::Avalonia.Application.Current is { } app)
        {
            app.RequestedThemeVariant = EffectiveVariant;
        }
    }

    /// <summary>
    /// Resolve AppTheme.System to a concrete ThemeVariant via the OS.
    /// Avalonia's PlatformSettings.GetColorValues().ThemeVariant returns
    /// ThemeVariant.Default if unknown — fall back to Dark.
    /// </summary>
    private static ThemeVariant ResolveSystemVariant()
    {
        try
        {
            if (global::Avalonia.Application.Current is { } app &&
                app.PlatformSettings is { } settings)
            {
                var os = settings.GetColorValues().ThemeVariant;
                if (os == PlatformThemeVariant.Light) return ThemeVariant.Light;
                if (os == PlatformThemeVariant.Dark) return ThemeVariant.Dark;
            }
        }
        catch { /* fall through */ }

        return ThemeVariant.Dark;
    }

    private static void Save()
    {
        try
        {
            Directory.CreateDirectory(Path.GetDirectoryName(PrefPath)!);
            File.WriteAllText(PrefPath, CurrentTheme switch
            {
                AppTheme.Light => "light",
                AppTheme.System => "system",
                _ => "dark",
            });
        }
        catch { /* non-critical */ }
    }
}
```

Note the `using global::Avalonia.Platform;` is not needed — `PlatformThemeVariant` is in the `Avalonia.Platform` namespace but is accessible via the settings instance's `GetColorValues()` call — the return type is `PlatformColorValues` which exposes `ThemeVariant` of type `PlatformThemeVariant`. Add `using global::Avalonia.Platform;` explicitly if the symbol doesn't resolve.

- [ ] **Step 4: Update `SettingsView.axaml.cs` callers**

Path: `src/UI/AuraCore.UI.Avalonia/Views/Pages/SettingsView.axaml.cs`

The old code uses `ThemeService.IsDarkMode` for a text label + `ThemeService.Toggle()` for a click handler. Since we're replacing the UI with a radio group in Task 3, the code-behind also changes. For now (Task 2), keep the existing label and toggle working against the new API. No file edit needed — `IsDarkMode` back-compat property keeps readers happy, `Toggle()` still exists.

- [ ] **Step 5: Run ThemeServiceTests — all 7 pass**

```bash
dotnet test tests/AuraCore.Tests.UI.Avalonia/AuraCore.Tests.UI.Avalonia.csproj --filter "FullyQualifiedName~ThemeServiceTests" --nologo 2>&1 | tail -3
```

Expected: `Passed! - Failed: 0, Passed: 7`.

- [ ] **Step 6: Full solution test**

```bash
dotnet test AuraCorePro.sln --verbosity minimal --nologo 2>&1 | grep -E "^(Passed!|Failed!)" | tail -10
```

Expected: 8 `Passed!` lines, 2259 total (2252 + 7 ThemeService tests).

- [ ] **Step 7: Commit**

```bash
git add src/UI/AuraCore.UI.Avalonia/ThemeService.cs tests/AuraCore.Tests.UI.Avalonia/Services/ThemeServiceTests.cs
git commit -m "feat(phase-6.3.B): ThemeService 3-state refactor — AppTheme {Dark,Light,System} + OS resolve"
```

---

# Sub-wave 6.3.C — Settings UI + App.axaml cleanup

## Task 3: Replace theme toggle with radio group + remove hardcoded Dark from App.axaml

**Files:**
- Modify: `src/UI/AuraCore.UI.Avalonia/App.axaml`
- Modify: `src/UI/AuraCore.UI.Avalonia/Views/Pages/SettingsView.axaml`
- Modify: `src/UI/AuraCore.UI.Avalonia/Views/Pages/SettingsView.axaml.cs`

- [ ] **Step 1: Remove hardcoded Dark from App.axaml**

Open `src/UI/AuraCore.UI.Avalonia/App.axaml` and delete the `RequestedThemeVariant="Dark"` attribute from the `<Application>` root element (line 5). The root should look like:

```xml
<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="using:AuraCore.UI.Avalonia.Converters"
             x:Class="AuraCore.UI.Avalonia.App">
```

ThemeService.Initialize() (called from `App.OnFrameworkInitializationCompleted` at `App.axaml.cs:302`) now sets `RequestedThemeVariant` to the persisted value before MainWindow shows.

- [ ] **Step 2: Find the theme section in SettingsView.axaml**

```bash
grep -n "ThemeLabel\|Theme\|settings.chooseTheme\|set.theme" src/UI/AuraCore.UI.Avalonia/Views/Pages/SettingsView.axaml
```

Note the surrounding structure (what panel/grid cell holds the theme widget).

- [ ] **Step 3: Replace the theme widget in SettingsView.axaml**

Find the existing theme region (a TextBlock named `ThemeLabel` or similar, clickable). Replace it with a radio-group block.

Radio group markup (preserve parent StackPanel/Grid structure — only replace the widget itself):

```xml
<StackPanel Spacing="6">
  <TextBlock x:Name="ThemeHeader"
             FontSize="{DynamicResource FontSizeLabel}"
             Foreground="{DynamicResource TextMutedBrush}"/>
  <TextBlock x:Name="ThemeDesc"
             FontSize="{DynamicResource FontSizeBody}"
             Foreground="{DynamicResource TextSecondaryBrush}"/>
  <StackPanel Orientation="Horizontal" Spacing="14" Margin="0,8,0,0">
    <RadioButton x:Name="ThemeSystemRb" GroupName="Theme"/>
    <RadioButton x:Name="ThemeLightRb"  GroupName="Theme"/>
    <RadioButton x:Name="ThemeDarkRb"   GroupName="Theme"/>
  </StackPanel>
</StackPanel>
```

Radio content is set via code-behind using `LocalizationService.Get(...)` because the existing `SettingsView.axaml.cs` uses code-set localization (confirmed via grep).

- [ ] **Step 4: Update SettingsView.axaml.cs**

Open `src/UI/AuraCore.UI.Avalonia/Views/Pages/SettingsView.axaml.cs`. In the `OnLoaded` / localization-refresh path:

1. Remove the old `ThemeLabel.Text = ThemeService.IsDarkMode ? ... : ...` lines (4 occurrences).
2. Set the radio button content + header labels:
   ```csharp
   ThemeHeader.Text = LocalizationService.Get("set.theme");
   ThemeDesc.Text = LocalizationService.Get("settings.chooseTheme");
   ThemeSystemRb.Content = LocalizationService.Get("settings.systemDefault");
   ThemeLightRb.Content = LocalizationService.Get("settings.light");
   ThemeDarkRb.Content = LocalizationService.Get("settings.dark");
   ```
3. Set the initial checked state based on `ThemeService.CurrentTheme`:
   ```csharp
   switch (ThemeService.CurrentTheme)
   {
       case ThemeService.AppTheme.System: ThemeSystemRb.IsChecked = true; break;
       case ThemeService.AppTheme.Light:  ThemeLightRb.IsChecked  = true; break;
       default:                           ThemeDarkRb.IsChecked   = true; break;
   }
   ```
4. Wire each radio's `IsCheckedChanged` handler (or `Checked` event):
   ```csharp
   ThemeSystemRb.IsCheckedChanged += (_, _) => { if (ThemeSystemRb.IsChecked == true) ThemeService.SetTheme(ThemeService.AppTheme.System); };
   ThemeLightRb.IsCheckedChanged  += (_, _) => { if (ThemeLightRb.IsChecked  == true) ThemeService.SetTheme(ThemeService.AppTheme.Light);  };
   ThemeDarkRb.IsCheckedChanged   += (_, _) => { if (ThemeDarkRb.IsChecked   == true) ThemeService.SetTheme(ThemeService.AppTheme.Dark);   };
   ```
5. Remove the old `OnThemeToggleClick` handler (if it exists) and any wiring that referenced `ThemeLabel`.

Keep the code-behind minimal — same pattern as the existing language radios if the file already uses RadioButtons for language, or the pattern above if not.

- [ ] **Step 5: Build**

```bash
dotnet build src/UI/AuraCore.UI.Avalonia/AuraCore.UI.Avalonia.csproj --nologo 2>&1 | tail -5
```

Expected: 0 errors.

- [ ] **Step 6: Full solution test**

```bash
dotnet test AuraCorePro.sln --verbosity minimal --nologo 2>&1 | grep -E "^(Passed!|Failed!)" | tail -10
```

Expected: 8 `Passed!` lines, 2259 total (no test-count change expected — SettingsView doesn't have existing render tests that would break).

- [ ] **Step 7: Commit**

```bash
git add src/UI/AuraCore.UI.Avalonia/App.axaml \
        src/UI/AuraCore.UI.Avalonia/Views/Pages/SettingsView.axaml \
        src/UI/AuraCore.UI.Avalonia/Views/Pages/SettingsView.axaml.cs
git commit -m "feat(phase-6.3.C): Settings theme radio group + App.axaml variant handoff to ThemeService"
```

---

# Sub-wave 6.3.D — Pixel gallery Light variant

## Task 4: `DesignSystemGalleryLightPixelTests` + accept 2 Light goldens

**Files:**
- Create: `tests/AuraCore.Tests.UI.Avalonia/PixelRegression/DesignSystemGalleryLightPixelTests.cs`
- Create: 4 goldens in `tests/AuraCore.Tests.UI.Avalonia/goldens/`

- [ ] **Step 1: Write the Light pixel test class**

Path: `tests/AuraCore.Tests.UI.Avalonia/PixelRegression/DesignSystemGalleryLightPixelTests.cs`

```csharp
using System.Threading.Tasks;
using AuraCore.Tests.UI.Avalonia.TestViews;
using AuraCore.UI.Avalonia;
using Avalonia.Headless.XUnit;
using Avalonia.Threading;
using Xunit;

namespace AuraCore.Tests.UI.Avalonia.PixelRegression;

/// <summary>
/// Light-variant snapshots of the design-system gallery. Mirrors
/// DesignSystemGalleryPixelTests (Phase 6.2) but applies AppTheme.Light
/// at test start and restores AppTheme.Dark at teardown to avoid leaking
/// theme state into other tests.
/// </summary>
[Trait("Category", "PixelRegression")]
public class DesignSystemGalleryLightPixelTests
{
    [AvaloniaFact]
    public async Task Gallery_wide_light()
    {
        var prev = ThemeService.CurrentTheme;
        ThemeService.SetTheme(ThemeService.AppTheme.Light);
        Dispatcher.UIThread.RunJobs();   // Let RequestedThemeVariant settle.
        try
        {
            var png = await PixelRegressionHarness.RenderViewAsync<DesignSystemGallery>(1200, 900);
            await PixelVerify.Verify(png);
        }
        finally
        {
            ThemeService.SetTheme(prev);
            Dispatcher.UIThread.RunJobs();
        }
    }

    [AvaloniaFact]
    public async Task Gallery_narrow_light()
    {
        var prev = ThemeService.CurrentTheme;
        ThemeService.SetTheme(ThemeService.AppTheme.Light);
        Dispatcher.UIThread.RunJobs();
        try
        {
            var png = await PixelRegressionHarness.RenderViewAsync<DesignSystemGallery>(800, 1100);
            await PixelVerify.Verify(png);
        }
        finally
        {
            ThemeService.SetTheme(prev);
            Dispatcher.UIThread.RunJobs();
        }
    }
}
```

- [ ] **Step 2: First run — expect 2 failures ("no verified golden")**

```bash
dotnet test tests/AuraCore.Tests.UI.Avalonia/AuraCore.Tests.UI.Avalonia.csproj --filter "FullyQualifiedName~DesignSystemGalleryLightPixelTests" --nologo 2>&1 | tail -15
```

Expected: 2 fail with Verify assertion error; `.received.png` + `.received.txt` written to `tests/AuraCore.Tests.UI.Avalonia/goldens/`.

- [ ] **Step 3: Inspect the `.received.png` visually**

```bash
ls tests/AuraCore.Tests.UI.Avalonia/goldens/ | grep Light
```

Open `DesignSystemGalleryLightPixelTests.Gallery_wide_light.received.png`. Confirm:
- Off-white page background (not dark-navy)
- Dark text on light surfaces (not light-on-dark)
- Status surfaces (SUCCESS/WARNING/ERROR/INFO) show tinted bands on white with darker semantic text
- Typography ramp readable in dark ink
- Accent swatches visible against light bg

If any surface is black/blank, the Light theme dict didn't apply — debug by checking `AvaloniaTestApplication` properly resolves `ThemeVariant.Light` and the Light dict loaded.

- [ ] **Step 4: Accept the 4 Light goldens**

```bash
cd tests/AuraCore.Tests.UI.Avalonia/goldens
mv DesignSystemGalleryLightPixelTests.Gallery_wide_light.received.png   DesignSystemGalleryLightPixelTests.Gallery_wide_light.verified.png
mv DesignSystemGalleryLightPixelTests.Gallery_wide_light.received.txt   DesignSystemGalleryLightPixelTests.Gallery_wide_light.verified.txt
mv DesignSystemGalleryLightPixelTests.Gallery_narrow_light.received.png DesignSystemGalleryLightPixelTests.Gallery_narrow_light.verified.png
mv DesignSystemGalleryLightPixelTests.Gallery_narrow_light.received.txt DesignSystemGalleryLightPixelTests.Gallery_narrow_light.verified.txt
cd ../../..
```

- [ ] **Step 5: Re-run — expect 2 passes**

```bash
dotnet test tests/AuraCore.Tests.UI.Avalonia/AuraCore.Tests.UI.Avalonia.csproj --filter "FullyQualifiedName~DesignSystemGalleryLightPixelTests" --nologo 2>&1 | tail -3
```

Expected: `Passed! - Failed: 0, Passed: 2`.

- [ ] **Step 6: 3× consecutive flake check**

```bash
cd /c/Users/Admin/Desktop/AuraCorePro/AuraCorePro
for i in 1 2 3; do
  echo "=== run $i ==="
  dotnet test tests/AuraCore.Tests.UI.Avalonia/AuraCore.Tests.UI.Avalonia.csproj --filter "FullyQualifiedName~DesignSystemGalleryLightPixelTests" --nologo 2>&1 | tail -2
done
```

Expected: 3 clean runs, all `Passed! - Failed: 0, Passed: 2`.

- [ ] **Step 7: Full solution test (confirm Dark gallery still green)**

```bash
dotnet test AuraCorePro.sln --verbosity minimal --nologo 2>&1 | grep -E "^(Passed!|Failed!)" | tail -10
```

Expected: 8 `Passed!` lines, 2261 total (2259 + 2 Light gallery). Verify `AuraCore.Tests.UI.Avalonia.dll` shows 1620 passed.

- [ ] **Step 8: Commit**

```bash
git add tests/AuraCore.Tests.UI.Avalonia/PixelRegression/DesignSystemGalleryLightPixelTests.cs \
        tests/AuraCore.Tests.UI.Avalonia/goldens/DesignSystemGalleryLightPixelTests.*.verified.png \
        tests/AuraCore.Tests.UI.Avalonia/goldens/DesignSystemGalleryLightPixelTests.*.verified.txt
git commit -m "feat(phase-6.3.D): DesignSystemGallery Light-variant pixel tests (2 goldens)"
```

---

# Sub-wave 6.3.E — Ceremonial close

## Task 5: Memory file + MEMORY.md + ceremonial commit + merge to main

**Files:**
- Create: `C:/Users/Admin/.claude/projects/C--/memory/project_phase_6_item_3_light_theme_complete.md`
- Modify: `C:/Users/Admin/.claude/projects/C--/memory/MEMORY.md`

- [ ] **Step 1: Write the memory file**

Path: `C:/Users/Admin/.claude/projects/C--/memory/project_phase_6_item_3_light_theme_complete.md`

Template (fill SHAs after the ceremonial commit in Step 3):

```markdown
---
name: Phase 6 Item 3 — Light Theme COMPLETE
description: Light theme shipped. Dark/Light/System 3-state in ThemeService. AuraCoreThemeV2 has Light token dictionary sibling. Settings radio group. App.axaml hardcoded Dark removed. Phase 6.2 gallery Light variant = 4 goldens total. Branch phase-6-light-theme merged to main at <merge-sha>. 2261 passing.
type: project
---

# Phase 6 Item 3 — Light Theme COMPLETE

**Status:** Implementation complete. (If merged: Branch `phase-6-light-theme` → main at `<merge-sha>`. Else: `<N> commits ahead of main f931020`.)

## Commits

| SHA | Scope |
|---|---|
| (ceremonial) | Memory + MEMORY.md + ceremonial commit |
| (6.3.D)      | DesignSystemGallery Light pixel tests + 4 goldens |
| (6.3.C)      | Settings radio group + App.axaml variant handoff |
| (6.3.B)      | ThemeService 3-state refactor |
| (6.3.A)      | Light token dictionary + glow split |
| (spec)       | Phase 6.3 Light theme design spec |

## Architecture

- `ThemeDictionaries[Dark]` + `ThemeDictionaries[Light]` in `AuraCoreThemeV2.axaml`.
- `ThemeService.AppTheme { Dark, Light, System }` + `CurrentTheme` + `EffectiveVariant`.
- System → OS resolve via `PlatformSettings.GetColorValues().ThemeVariant`.
- Persistence: `%LOCALAPPDATA%/AuraCorePro/theme.pref` ("dark"|"light"|"system").
- Settings radio group (3 options) replaces old 2-state toggle.
- 2 Light-variant pixel tests use setup/teardown to flip theme + restore.

## Test-count delta

- Baseline 2252 → Target 2261 (+9)
- +7 ThemeServiceTests (unit)
- +2 Light gallery pixel tests

## Carry-forward

- Per-module hardcoded dark-color audit (any `Foreground="White"` still in XAML) — defer to Phase 6.x debt sweep.
- Mid-session OS-theme auto-follow — intentionally out of scope; System resolves once at startup.
- HDR / high-contrast modes — future accessibility item.

## Next roadmap action

Phase 6 Item 4 — TR completion sweep. Per roadmap: "ensure every in-app string is Turkish when Turkish is selected" (not language-set expansion).
```

- [ ] **Step 2: Update MEMORY.md index**

Demote the Phase 6 Item 2 entry to "authoritative for own history, superseded by Item 3". Add new Item 3 entry. Update Phase 6 Roadmap entry.

Example diffs:

Change the Phase 6 Item 2 line from:
```
- [Phase 6 Item 2 ...] — **CURRENT STATE.** ...
```
to:
```
- [Phase 6 Item 2 ...] — Superseded by Item 3 for current state; authoritative for Item 2 history. ...
```

Add new line for Item 3 marking it **CURRENT STATE.**

Update the Phase 6 Roadmap line to "Items 1+2+3 done; next action: Item 4 (TR completion sweep) brainstorm."

- [ ] **Step 3: Ceremonial commit**

```bash
cd C:/Users/Admin/Desktop/AuraCorePro/AuraCorePro
git commit --allow-empty -m "milestone: Phase 6 Item 3 Light Theme COMPLETE"
git rev-parse --short HEAD
```

Note the SHA for backfill.

- [ ] **Step 4: Backfill ceremonial SHA into memory file**

Replace `(ceremonial)` placeholder in `project_phase_6_item_3_light_theme_complete.md` with the SHA from Step 3.

- [ ] **Step 5: Merge to main**

```bash
git checkout main
git merge --no-ff phase-6-light-theme -m "Merge branch 'phase-6-light-theme' — Phase 6 Item 3 Light Theme COMPLETE

Dark/Light/System theme switcher with full token duality. 3-state
ThemeService with OS-theme detection. Settings radio group. 2 Light-
variant pixel tests extending the Phase 6.2 gallery for cross-theme
regression catching.

+9 tests (2252 → 2261). No new NuGet packages. 5 sub-waves merged."
git log --oneline -5
```

- [ ] **Step 6: Verify main is green**

```bash
dotnet test AuraCorePro.sln --verbosity minimal --nologo 2>&1 | grep -E "^(Passed!|Failed!)" | tail -10
```

Expected: 8 `Passed!` lines, 2261 total.

- [ ] **Step 7: Backfill merge SHA into memory file**

Edit `project_phase_6_item_3_light_theme_complete.md` and the MEMORY.md entry — replace `<merge-sha>` with the actual merge-commit SHA from Step 5.

```bash
git rev-parse --short HEAD
```

---

## Plan summary

- **Tasks:** 5 (1 token-dict + 1 service refactor + 1 UI/app integration + 1 pixel-test + 1 close)
- **Estimated effort:** 1.5-2 days
- **Expected test growth:** 2252 → 2261 (+7 ThemeServiceTests, +2 Light gallery)
- **New NuGet packages:** 0
- **Branch:** `phase-6-light-theme` from main `f931020`; merge via `--no-ff` on completion

## Self-review

**Spec coverage:**
- Spec §scope #1 "Light token dict" → Task 1 ✓
- Spec §scope #2 "ThemeService refactor" → Task 2 ✓
- Spec §scope #3 "System detection" → Task 2 (ResolveSystemVariant) ✓
- Spec §scope #4 "persistence migration" → Task 2 (Initialize + Save) ✓
- Spec §scope #5 "Settings UI radio group" → Task 3 ✓
- Spec §scope #6 "App.axaml remove hardcoded Dark" → Task 3 ✓
- Spec §scope #7 "pixel gallery Light variant" → Task 4 ✓
- Spec §decisions #1-16 → all encoded in Task code blocks

**Placeholder scan:** Clean. `(ceremonial)` + `<merge-sha>` in Task 5 are intentional post-commit backfills.

**Type consistency:**
- `ThemeService.AppTheme.{Dark,Light,System}` consistent across Tasks 2, 3, 4
- `ThemeService.CurrentTheme` / `EffectiveVariant` / `IsDarkMode` / `SetTheme` / `Toggle` consistent
- `PixelRegressionHarness.RenderViewAsync<TView>(w, h)` + `PixelVerify.Verify(png)` match Phase 6.2 signature
- `[Trait("Category", "PixelRegression")]` applied consistently on gallery tests

**Known gaps / judgment calls:**
- Spec mentions "brief dark-flash on startup avoided because ThemeService.Initialize runs before window creation" — confirmed: `App.axaml.cs:302` initializes in `OnFrameworkInitializationCompleted` before `MainWindow` creation. No task action needed.
- Pixel-test flake risk: `Dispatcher.UIThread.RunJobs()` after `SetTheme` is defensive; if `RequestedThemeVariant` propagation is synchronous in Avalonia 11.2.7, the RunJobs call is a no-op but cheap.
- Step 3 of Task 3 says "Find the existing theme region" — the implementer must grep/read `SettingsView.axaml` to find the exact surrounding markup. Intentional, not a placeholder — the file may have evolved in ways the spec can't fully encode.

---

**After Phase 6.3:** Phase 6 Item 4 (TR completion sweep) brainstorm per roadmap.
