# Phase 5.6 Ceremonial Close Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close Phase 5 (Wave 3: Polish + Cross-Cutting Infra) ceremonially: write the umbrella retrospective memory file, demote the Phase 5.5 MEMORY.md entry to history and add the Phase 5 close as CURRENT STATE, land the ceremonial empty commit. Defer the `phase-5-polish` → `main` merge decision to the user.

**Architecture:** Three mechanical steps on branch `phase-5-polish`. No source/test changes — this is pure bookkeeping.

**Tech Stack:** Markdown · Git (empty commit) · MEMORY.md index at `C:/Users/Admin/.claude/projects/C--/memory/MEMORY.md`

**Spec reference:** `docs/superpowers/specs/2026-04-17-phase5.6-ceremonial-close-design.md` (committed `282d291`)

**Baseline:** Branch `phase-5-polish` HEAD `282d291` (spec on top of `83266a3` Phase 5.5 close). 2141 passing + 6 skipped + 0 failed. No test delta expected this wave.

---

## File Structure

### Created

```
C:/Users/Admin/.claude/projects/C--/memory/project_ui_rebuild_phase_5_close.md
```

### Modified

```
C:/Users/Admin/.claude/projects/C--/memory/MEMORY.md
```

### Repository state (live ops, not committed to repo)

```
New empty commit on phase-5-polish with subject "milestone: Phase 5 Polish + Cross-Cutting Infra Wave 3 CLOSED"
```

---

## Task 1: Write `project_ui_rebuild_phase_5_close.md`

**Files:**
- Create: `C:/Users/Admin/.claude/projects/C--/memory/project_ui_rebuild_phase_5_close.md`

- [ ] **Step 1: Create the memory file**

Use the Write tool to create `C:/Users/Admin/.claude/projects/C--/memory/project_ui_rebuild_phase_5_close.md` with the content below. Exact content — no placeholders, no edits:

```markdown
---
name: AuraCorePro UI Rebuild — Phase 5 Wave 3 CLOSED (Polish + Cross-Cutting Infra)
description: Umbrella close for Phase 5. Five sub-waves (5.1 cleanup → 5.2 privilege IPC → 5.3 narrow-mode → 5.4 Phase 3 debt → 5.5 QA features) retired. 2141/2141 tests + 6 skipped on branch `phase-5-polish`. Merge-to-main deferred to user (invoke `superpowers:finishing-a-development-branch` when ready).
type: project
originSessionId: 2026-04-17-phase5.6-ceremonial-close-afk
---

# UI Rebuild — Phase 5 CLOSE (Wave 3: Polish + Cross-Cutting Infra)

**Wave close date:** 2026-04-17 (AFK autonomous ceremonial)
**Branch:** `phase-5-polish` (285+ commits ahead of `main` at close)
**Phase 5 ceremonial HEAD:** `<fill at commit time>` (see `git log --oneline -1 phase-5-polish`)
**Tests:** 2141 passing + 6 skipped + 0 failed across 8 assemblies
**Net test growth this phase:** +583 (from 1558 at Phase 4.4 close to 2141 at Phase 5 close)

## Phase 5 scope — five sub-waves shipped

| Sub-wave | Scope | Milestone | Test delta |
|---|---|---|---|
| 5.1 | V2 token cleanup + FontManager soft-hide + Windows app-installer sidebar move | `1b00ddb` | 1558 → 1577 (+19) |
| 5.2.0 | Abstraction wave (`IShellCommandService` + 4 platform impls + DI + UX primitives) | `90c4640` | 1577 → 1601 (+24) |
| 5.2.1 | Linux privilege IPC (Tmds.DBus + auracore-privhelper daemon + polkit + 3 engine migrations) | `8f798dc` | 1601 → 1703 (+102) |
| 5.2.2 | macOS privilege IPC (SMAppService + XPC helper + 4 engine migrations) | `21f6d9c` | 1703 → 1893 (+190) |
| 5.2 close | Privilege IPC wave sealed | `0045573` | — |
| 5.3 | Narrow-mode responsive wave (INarrowModeService + converters + StatRow + AIFeaturesView + 16-module audit) | `1a8e882` | 1893 → 1989 (+96) |
| 5.4 | Phase 3 debt polish (11 items across 3 sub-waves + live HSTS cleanup) | `16c1ce1` | 1989 → 2032 (+43) |
| 5.5 | QA features wave (9 items + Windows Named Pipe server + new ServiceManager module) | `83266a3` | 2032 → 2141 (+109) |
| 5.6 | Ceremonial close (this file) | `<fill at commit>` | — |

## Key architectural additions (consolidated across Phase 5)

1. **`IShellCommandService`** abstraction with 4 impls (Windows, Linux, macOS, InProcess). Unified privileged-command surface across platforms; engines hit one API, platform-specific IPC hides behind it.
2. **Named Pipe server on Windows** (`AuraCore.PrivilegedService.exe`, runs as LocalSystem) with ACL + 13-action whitelist + 64KB request cap + CorrelationId protocol.
3. **polkit daemon on Linux** (`auracore-privhelper`, Tmds.DBus) with per-action whitelist.
4. **XPC helper on macOS** (`com.auracore.privhelper`, SMAppService install flow).
5. **`INarrowModeService`** singleton observing Window.Bounds + converter chain for responsive layouts.
6. **`INavigationService`** singleton for `ai-*` + `disk-health` deep-link routing (Phase 5.4).
7. **`IAuraCoreLLM.ReloadAsync` + IsReloading INPC** with concurrent-call guard + inference drain + cancellation (Phase 5.4 architectural hook).
8. **`QuickActionTileVM` + `QuickActionPresets.Windows`** (Dashboard refactor, Phase 5.5).
9. **`DirectoryNodeVM` + `SpaceAnalyzerScanner`** (tree drill-down, Phase 5.5).
10. **`DiskHealthScanner`** helper (live SMART-style data, Phase 5.5).
11. **`SymlinkArgvValidator`** (strict validator for `symlink.create` action, Phase 5.5).
12. **`ActionWhitelist.Windows` + `ArgumentValidator`** (defense-in-depth Ops gate, Phase 5.5).
13. **V1 theme bridge DELETED** (`AuraCoreTheme.axaml` retired; V2 tokens only in XAML, Phase 5.1).
14. **NEW module: `AuraCore.Module.ServiceManager`** (first new Phase 5 module — Start/Stop/Restart/SetStartup via IShellCommandService).
15. **`LocalizationParityTests`** regression guard (EN/TR key parity enforcement, Phase 5.4).

## New NuGet packages introduced this phase

- `Tmds.DBus` 0.92.0 (5.2.1 Linux D-Bus client; pinned to fix CVE-2026-39959)
- `System.IO.Pipes.AccessControl` 5.0.0 (5.5.1 Windows pipe ACL)
- `Microsoft.Extensions.Hosting` 8.0.0 (5.5.1 PrivilegedService BackgroundService)
- `System.ServiceProcess.ServiceController` 8.0.0 (5.5.1 new ServiceManager module)
- `Microsoft.Extensions.Logging.Abstractions` 8.0.0 (5.5.1 IPC logging)

Total: 5 new packages across the phase. All Microsoft-published or widely-trusted (Tmds.DBus) where BCL doesn't suffice.

## Carry-forward debt (NOT addressed in Phase 5)

### Phase 5.2 Linux
- **GrubManager 3 deferred sudo hits** (`TODO(phase-5.2.1)` markers at lines 199, 320, 373 in `GrubManagerModule.cs`). Need new validator sub-actions: `backup-etc-grub` + `grub-mkconfig`.

### Phase 5.2 macOS
- **TimeMachineManager 2 deferred verbs** (`TODO(phase-5.2.2)` markers): `thinlocalsnapshots` needs `purgeable` action id; `tmutil delete` needs path-strict validator.
- **PurgeableSpaceManager `run-periodic` dead-code branch** — remove OR add new validator sub-action.
- **SwapOptimizer migration** — explicitly deferred from 5.2.2 (mini-spec §3.8).
- **macOS daemon ObjC block-handler wiring** (Task 26 follow-up) — `xpc_connection_set_event_handler` needs Swift/C shim `.dylib`.
- **SMAppService NSError out-param robustness** (Task 30 follow-up) — may need Swift shim for reliable error marshalling.
- **TEAM_ID signing-time substitution** (placeholder `"AURACORE_TEAM_ID_PLACEHOLDER"` in `SecurityConfig.cs`) — ship-prep.
- **macOS notarization + `lipo` universal binary pipeline** — ship-prep.

### Phase 5.3 narrow-mode
- **AIFeaturesView icon-only compression at 80 DIP** (`TODO(phase-5.3.2.3)`). Current compact text ("Recs"); full icon-glyph substitution needs per-nav-item icon assets OR DataTemplate swap.
- **6 pilot-view render tests skipped** (`TODO(phase-5.3.3)`): SystemHealth/BloatwareRemoval/RamOptimizer views require `App.Services` DI root which isn't bootstrapped in headless test harness.

### Phase 5.4
- **`ChatSection.axaml.cs` wire-up of `ReloadAsync`** — VM hook exists since `bd47650`; UI click still advises restart.
- **Nginx `auracore-api.bak` stale file in `sites-enabled/`** at origin `165.227.170.3` — causes non-fatal "conflicting server name" warnings.
- **API conf duplicate `add_header Strict-Transport-Security`** (3 locations) — pre-existing, consolidation pending.

### Phase 5.5
- **WorstTemp "—"** in DiskHealthSummaryCard — `DriveInfo` has no temperature; extension needs WMI `MSStorageDriver_ATAPISmartData` or `smartctl`.
- **Driver update execution** — Phase 5.5 scoped to scan + backup; full Windows Update Agent COM API install still out.
- **Linux/macOS QuickAction preset catalogs** — only Windows presets this wave.
- **Windows Named Pipe end-user install UX** — `scripts/install-privileged-service.ps1` still admin-only; no in-app first-run elevation prompt.

### Cross-wave
- **Pixel-regression / visual snapshot testing** — Phase 1 carry-forward, infrastructure gap.
- **AuraCore.Desktop WinUI build error** (dotnet SDK 10.0.201 missing `MrtCore.PriGen.targets`/`AppxPackage` dll) — pre-existing since Phase 5.1 baseline; does not block tests.
- **Real VM validation on Ubuntu 24.04 + Fedora 40 + macOS 14 Sonoma** — manual QA, deferred.
- **Light theme / mobile + tablet layouts / localization beyond EN+TR / deep-link URL routing (`auracore://…`)** — Vision Doc §12 still deferred.

## Test count progression (Phase 4.4 close → Phase 5 close)

```
Phase 4.4 close:       1558 passing
Phase 5.1 close:       1577 passing (+19)
Phase 5.2.0 close:     1601 passing (+24)
Phase 5.2.1 close:     1703 passing (+102)
Phase 5.2.2 close:     1893 passing (+190)
Phase 5.3 close:       1989 passing (+96)
Phase 5.4 close:       2032 passing (+43)
Phase 5.5 close:       2141 passing (+109)  ← current
Phase 5.6 ceremonial:  2141 passing + 6 skipped + 0 failed  (no delta — bookkeeping only)
```

## Execution methodology notes

- **Single long-lived branch** `phase-5-polish` (from Phase 4.4 `a2062c9`) hosted all 5 sub-waves + their milestones. No feature branches inside the wave.
- **Subagent-driven development** throughout (`feedback_subagent_driven_default.md`). Haiku for mechanical, sonnet for integration/design/security-critical work. Main-session inline verification replaced formal two-stage review for mechanical tasks.
- **Supervisor mode** active (`feedback_supervisor_mode.md`): main-session verification after each commit; formal `Read` for security-critical files (PipeServer.cs, ACL setup, validators).
- **Spec review gate skipped** per `feedback_skip_spec_review.md` throughout.
- **AFK autonomous mode** activated on Phase 5.2 start and again on Phase 5.5 start (session-bounded); recommended defaults applied at every decision, ~20+ locked decisions across the phase.
- **Zero test regressions** across the phase. Every sub-wave milestone landed with full `dotnet test AuraCorePro.sln` green.

## Memory files in play at Phase 5 close

1. **This file** — Phase 5 close umbrella (CURRENT STATE)
2. `project_ui_rebuild_phase_5_5_qa_features_complete.md` — Phase 5.5 history (authoritative for 5.5)
3. `project_ui_rebuild_phase_5_4_debt_polish_complete.md` — Phase 5.4 history
4. `project_ui_rebuild_phase_5_3_narrow_mode_complete.md` — Phase 5.3 history
5. `project_ui_rebuild_phase_5_2_privilege_ipc_complete.md` — Phase 5.2 close (3 sub-waves)
6. `project_ui_rebuild_phase_5_2_0_abstraction_complete.md` — Phase 5.2.0 history
7. `project_ui_rebuild_phase_5_2_1_linux_complete.md` — Phase 5.2.1 history
8. `project_ui_rebuild_phase_5_1_cleanup_complete.md` — Phase 5.1 history
9. `project_ui_rebuild_phase_4_4_macos_modules_complete.md` — Phase 4.4 (Wave 2 close) history
10. Earlier phase histories (4.3, 4, 3, 2, 1)

Behavior preference memory files still active for next session (unchanged): `feedback_subagent_driven_default.md`, `feedback_skip_spec_review.md`, `feedback_supervisor_mode.md`, `feedback_gpu_cpu_limit.md`, `feedback_notify_before_gpu.md`, `feedback_visual_companion_always_yes.md`, `feedback_afk_default_recommended.md` (dormant).

## Next-session bootstrap

```bash
cd C:/Users/Admin/Desktop/AuraCorePro/AuraCorePro
git checkout phase-5-polish
git log --oneline -5
# Expected top: the Phase 5 ceremonial commit
dotnet test AuraCorePro.sln --verbosity minimal --nologo 2>&1 | grep -E "^Passed!"
# Expected: 8 Passed! lines, no Failed!, ~2141 total
```

## Merge decision — pending user input

**Branch `phase-5-polish` is 285+ commits ahead of `main`.** Merge strategy has NOT been decided autonomously (AFK guardrail: shared-state irreversible-ish operations need user sign-off).

When the user returns, the recommended next action is to invoke `superpowers:finishing-a-development-branch` to evaluate options:
1. Squash + merge to `main` (single feature commit for the entire Wave 3 body of work)
2. Merge commit preserving full sub-wave history (retains milestone granularity)
3. Rebase + fast-forward (linear history)
4. Open PR and keep branch alive pending review
5. Leave branch open indefinitely (further Phase-5+ waves continue accumulating on it)

No default chosen. User decides.

## Phase 6+ roadmap (not started)

Vision Doc references post-Phase-5 work:
- Light theme
- Mobile/tablet responsive layouts
- Localization beyond EN/TR
- Deep-link URL routing
- Pixel-regression testing infrastructure
- Production notarization + code-signing pipeline for macOS

None of these are in a Phase 6 spec yet. New brainstorm cycle when user is ready.
```

- [ ] **Step 2: Verify the file exists and is non-empty**

```bash
ls -la C:/Users/Admin/.claude/projects/C--/memory/project_ui_rebuild_phase_5_close.md
```

Expected: file size > 5 KB.

- [ ] **Step 3: No commit yet — combined with Task 3 ceremonial commit**

(Memory files live outside the repo working tree — they don't get committed to this repo. They're part of the user's persistent memory index. Skip `git add` for this path.)

---

## Task 2: Update `MEMORY.md` index

**Files:**
- Modify: `C:/Users/Admin/.claude/projects/C--/memory/MEMORY.md`

- [ ] **Step 1: Find the current Phase 5.5 line marked "CURRENT STATE"**

The MEMORY.md index currently contains:

```
- [UI Rebuild Phase 5.5 QA Features Wave COMPLETE](project_ui_rebuild_phase_5_5_qa_features_complete.md) — **CURRENT STATE.** All 9 Phase 4.2 QA feature ideas closed across 4 sub-waves (AFK autonomous). ...
```

- [ ] **Step 2: Edit the line — demote 5.5 and insert the Phase 5 close entry right after it**

Use the Edit tool. Replace the existing Phase 5.5 line with two lines: the demoted 5.5 entry + the new Phase 5 close entry. Exact diff:

**Find:**
```
- [UI Rebuild Phase 5.5 QA Features Wave COMPLETE](project_ui_rebuild_phase_5_5_qa_features_complete.md) — **CURRENT STATE.** All 9 Phase 4.2 QA feature ideas closed across 4 sub-waves (AFK autonomous). 5.5.1 Windows IPC foundation (10 tasks): ArgumentValidator + ActionWhitelist + IPC contracts + 3 Ops classes + PipeServer ACL+whitelist+dispatcher + WindowsShellCommandService client + install/uninstall PowerShell scripts + Driver Updater scan/export migration + Defender Manager 7 write actions + NEW AuraCore.Module.ServiceManager with row context menu. 5.5.2 UX quick wins (3 + 1 follow-up): Dashboard QuickAction tile collection + Bloatware preset; Disk Health → Dashboard widget + sidebar removal + live SMART-style data via DiskHealthScanner helper; Symlink find-vs-create split with SymlinkArgvValidator + polkit policy. 5.5.3 design docs (2): cleaner consolidation decision doc + subtitle disambiguation; System Health intro card. 5.5.4 Space Analyzer (1): TreeView drill-down + SpaceAnalyzerScanner + DirectoryNodeVM lazy-load + 2 new converters. Branch `phase-5-polish` HEAD `2afd3ef` (5.5.4 milestone; ceremonial close lands after memory update). **2141/2141 tests + 6 skipped** (+109 from 2032). New NuGet packages: System.IO.Pipes.AccessControl 5.0.0, Microsoft.Extensions.Hosting 8.0.0, System.ServiceProcess.ServiceController 8.0.0. Next: Phase 5.6 Phase 5 ceremonial close per umbrella roadmap.
```

**Replace with:**
```
- [UI Rebuild Phase 5.5 QA Features Wave COMPLETE](project_ui_rebuild_phase_5_5_qa_features_complete.md) — Superseded by Phase 5 close for current state; authoritative for Phase 5.5 history. 4 sub-waves (Windows IPC foundation + UX quick wins + design docs + Space Analyzer tree). Branch `phase-5-polish` HEAD `83266a3`. 2141/2141 tests (+109 from 2032).
- [UI Rebuild Phase 5 CLOSE (Wave 3 CLOSED)](project_ui_rebuild_phase_5_close.md) — **CURRENT STATE.** Umbrella close: 5 sub-waves retired (5.1 cleanup → 5.2 privilege IPC → 5.3 narrow-mode → 5.4 Phase 3 debt → 5.5 QA features). Net test growth +583 across Phase 5 (1558 → 2141). Key architecture: IShellCommandService + Windows Named Pipe + Linux polkit daemon + macOS XPC + INarrowModeService + INavigationService + IAuraCoreLLM.ReloadAsync + QuickActionTileVM + new AuraCore.Module.ServiceManager. 5 new NuGet packages. Branch `phase-5-polish` HEAD `<ceremonial>` (285+ commits ahead of main). 2141/2141 tests + 6 skipped. **Merge-to-main decision pending user** — invoke `superpowers:finishing-a-development-branch` when ready.
```

The `<ceremonial>` token is a placeholder — after Task 3's ceremonial commit lands, revisit this file and replace `<ceremonial>` with the actual commit SHA. See Task 3 Step 3.

- [ ] **Step 3: Verify the edit**

Read the MEMORY.md file; confirm:
- The Phase 5.5 line no longer has "**CURRENT STATE.**"
- A new Phase 5 CLOSE line exists below it with "**CURRENT STATE.**"
- No accidental deletions elsewhere in the file

```bash
grep -n "CURRENT STATE" C:/Users/Admin/.claude/projects/C--/memory/MEMORY.md
```

Expected: exactly one match, on the Phase 5 CLOSE line.

- [ ] **Step 4: No git commit for MEMORY.md**

MEMORY.md lives outside the repo — it's user persistent memory. Skip `git add`.

---

## Task 3: Ceremonial empty commit

**Files:** (none — empty commit)

- [ ] **Step 1: Run the ceremonial commit**

```bash
cd C:/Users/Admin/Desktop/AuraCorePro/AuraCorePro
git commit --allow-empty -m "milestone: Phase 5 Polish + Cross-Cutting Infra Wave 3 CLOSED"
```

Expected output:
```
[phase-5-polish <sha>] milestone: Phase 5 Polish + Cross-Cutting Infra Wave 3 CLOSED
```

- [ ] **Step 2: Capture the ceremonial commit SHA**

```bash
git rev-parse --short HEAD
```

Expected: a 7-char SHA. Note it for Step 3.

- [ ] **Step 3: Backfill the `<ceremonial>` placeholder in memory files**

Two files have `<ceremonial>` / `<fill at commit>` / `<fill at commit time>` tokens:

1. `C:/Users/Admin/.claude/projects/C--/memory/project_ui_rebuild_phase_5_close.md` — body has `<fill at commit time>` in the "Phase 5 ceremonial HEAD:" line AND in the test-count table row for 5.6
2. `C:/Users/Admin/.claude/projects/C--/memory/MEMORY.md` — the Phase 5 CLOSE line has `HEAD `<ceremonial>``

Replace each occurrence with the actual commit SHA from Step 2.

Use the Edit tool — `replace_all: true` where safe (for `<fill at commit>`-style tokens).

- [ ] **Step 4: Verify tokens are gone**

```bash
grep -n "<ceremonial>\|<fill at commit" C:/Users/Admin/.claude/projects/C--/memory/project_ui_rebuild_phase_5_close.md
grep -n "<ceremonial>" C:/Users/Admin/.claude/projects/C--/memory/MEMORY.md
```

Expected: both return empty (no matches).

- [ ] **Step 5: Verify the branch head + log**

```bash
git log --oneline -10 phase-5-polish
```

Expected top line: `<ceremonial-sha> milestone: Phase 5 Polish + Cross-Cutting Infra Wave 3 CLOSED`.

- [ ] **Step 6: No further commits**

The phase is closed. Do NOT attempt to merge to `main` — that's a pending user decision per the spec §3 Q1.

---

## Task 4: Final handoff note to user

**Files:** (none — conversational output)

- [ ] **Step 1: Surface a concise end-of-phase summary to the user**

Emit a single message summarizing:
- Phase 5 closed at ceremonial commit `<sha>`
- Branch `phase-5-polish` is 285+ commits ahead of `main`, still open
- User's next step when ready: `superpowers:finishing-a-development-branch` to decide merge strategy
- Full test state: 2141/2141 + 6 skipped, zero regressions across the whole wave
- New CURRENT STATE memory file: `project_ui_rebuild_phase_5_close.md`

Keep it ≤ 10 lines — user will scan this to pick up where things left off.

- [ ] **Step 2: Mark all TodoWrite items complete**

---

## Self-review

**Spec coverage:**
- Spec §2 deliverable 1 (memory file) → Task 1 ✓
- Spec §2 deliverable 2 (MEMORY.md demote + insert) → Task 2 ✓
- Spec §2 deliverable 3 (ceremonial commit) → Task 3 ✓
- Spec §5 success criteria (all 5 checkboxes) → covered across Tasks 1-4 ✓
- Spec §3 Q1 (merge deferred) → handled in Task 4 handoff note ✓

**Placeholder scan:** Clean. The `<ceremonial>` / `<fill at commit time>` tokens ARE intentional placeholders that MUST be backfilled (Task 3 Step 3) — this is a two-phase write intentional by design, not a plan failure.

**Type consistency:** No types in this plan — pure docs.

---

## Plan summary

- **Tasks:** 4 (1 memory-file write + 1 MEMORY.md edit + 1 empty commit + 1 handoff)
- **New commits on branch:** 1 (ceremonial empty commit)
- **Repository source/test changes:** 0
- **NuGet changes:** 0
- **Test delta:** 0 (still 2141 passing + 6 skipped)
- **User decisions deferred:** 1 (branch merge strategy)
- **Expected duration:** ≤ 10 minutes in a single AFK autonomous session

**After Phase 5.6:** Phase 6+ work is not scoped. User invokes `superpowers:finishing-a-development-branch` when ready to handle the merge.
